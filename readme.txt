the program uses csv for writing purposes, numpy for computations, and nltk as a toolbox
no other packages are in use.

The program is simple to run, simply input sentiment and file for each group of tweets(assuming tweets of different sentiments are kept in different files)

set each address, and that will create a classifier

at line 59 the address is that of the test set, feel free to chance name for something you like, but it must stay a .scv

This will result in s prediciton model of appropriate size and type